node_modules
dist
pnpm-lock.yaml
# .NET owns its own formatting via .editorconfig + dotnet format
backend
**/bin
**/obj

# spec-kit. Vendor-managed (refreshed by `specify self upgrade`) and generated
# spec artifacts that the speckit scripts parse; reformatting both only creates
# diff noise.
.specify
.claude/skills
specs

# Generated: React Router build output and route types, and the API wire types
# generated from the OpenAPI document. Formatting generated files only creates
# diff noise, and schema.d.ts is compared byte for byte by the drift check.
frontend/build
.react-router
frontend/src/api/schema.d.ts
