node_modules
dist
pnpm-lock.yaml
# .NET owns its own formatting via .editorconfig + dotnet format
backend
**/bin
**/obj

# spec-kit. Vendor-managed (refreshed by `specify self upgrade`) and generated
# spec artifacts that the speckit scripts parse; reformatting both only creates
# diff noise.
.specify
.claude/skills
specs
